# Working plan

This is the per-feature working-plan slot. For each enhancement/feature, the plan is
written here (then implemented test-first by a Sonnet subagent) and overwritten by the
next feature's plan. See `CLAUDE.md` → "Branches and workflow" for the full loop, and
`tasks/lessons.md` for accumulated conventions.

_No active plan — the MVP (original Tasks 1–8) is complete. Completed design/plan docs are
archived at `../oldplans/chooseyourown/`._
