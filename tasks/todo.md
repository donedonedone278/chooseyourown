# Feature: <none in progress>

_Working slot for the current feature's plan. Per `CLAUDE.md` → "Branches and
workflow": branch off `develop`, write the plan here, have Sonnet implement it
test-first until `npm test` is green, get user approval, then merge to `develop`.
Reset this file back to this placeholder after each merge so stale notes don't
leak into the next cycle. `tasks/lessons.md` is the durable record._

## Plan

_(empty)_

## Review

_(empty)_
